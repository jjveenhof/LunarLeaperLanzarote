# QGIS Session

BEFORE ANYTHING ELSE: read QandA.md in this directory.

This file is loaded by sessions opened in Code/QGIS/. The root CLAUDE.md
(loaded automatically alongside this) covers the overall project structure,
CRS, environment, and working conventions.

QandA.md entries directed here are tagged `From: [session] -> QGIS`.

The .qgz projects and their layer data live in the `QGIS project/` folder at the project
root (renamed 2026-08-11 from plain `QGIS/`, which collided with this code folder). From
this session's working directory that is `../../QGIS project/` (or use the full path).
Layer paths inside the .qgz are all RELATIVE (`./x.shp`, `../Data/...`), so the rename
did not break them -- but do not move that folder to a different depth.
QGIS .qgs/.qgz files are XML and readable directly. Geodata (shapefiles,
rasters) can be processed via Python with geopandas/rasterio.

## Overview
QGIS is used for geospatial visualisation: overview plots of data collection
locations, CRS EPSG:4083 (REGCAN95 / UTM zone 28N) visualisation, and
cross-method spatial context.

## Key Layers / Project Files

### QGIS Project Files (under QGIS project/ at project root)
- `FieldworkReporting.qgz` -- MAIN ACTIVE PROJECT. 35 layers, 7 print layouts, 4 of which
  are thesis figures (traceability table in `Code/QGIS/REFACTOR_FINDINGS.md`, or the
  project-level `TRACEABILITY.md` once the Supervisor session has consolidated all four
  sessions' tables into it). Despite the name, this is the current one -- open this file,
  not "Research module report.qgz".
- Legacy/broken, ignore: `Research module report.qgz` (stale since Feb -- 8 of its 10
  layers point at a "La Corona Cave" folder that no longer exists, contains zero print
  layouts and none of the overburden/DTM/GPR-line work below), `Fieldworkplanning*.qgz`
  (pre-fieldwork planning). See `Code/QGIS/REFACTOR_FINDINGS.md` finding 1 for the full
  audit trail.

### Rasters
- `QGIS project/cavebottom.tif`, `cavetop.tif` -- LiDAR-derived cave depth rasters (depth of cave ceiling/floor)
**MANUAL ARTIFACT -- cannot be regenerated by running anything.** The three overburden rasters
below are the product of hand-driven QGIS GUI steps (Align Rasters, the Processing raster
calculator, GDAL Rasterize), not a script. Nothing in this repo reproduces them unattended. A
successor CAN verify them -- the recipe is fully written out below, every input file is in
`QGIS project/`, and stats/sanity numbers are given per area to check against -- but cannot
regenerate them by running a command. If bit-for-bit reproduction is ever needed, budget GUI
time (roughly 15-20 min/area) rather than looking for a script that doesn't exist.

Roof-thickness (overburden) maps -- three areas, all EPSG:4083, 2 m cells. Recipe: overburden =
ground surface (`drone_topo.tif`) - cave ceiling; align surface onto the ceiling grid (Align Rasters,
bilinear, clip to ceiling extent -- OR just set reference layer = ceiling in the Processing raster
calculator, which resamples on the fly); subtract (ceiling nodata -> depth nodata, so it self-clips
to the footprint); mask jameos/open rims to nodata by burning `QGIS project/Jameos.shp` with GDAL Rasterize
(overwrite, value = nodata). GOTCHA: burn a SEPARATELY SAVED copy -- copy-pasting a layer in QGIS
keeps the same source file, so the burn hits the original. Style singleband pseudocolor 1-36 m,
clip-out-of-range (<1 m = jameos/negatives -> transparent). Raw pre-mask subtractions are kept next
to each final as `cavetop_clean*.tif` (no `_masked`). Ceilings are `caveheight_clean*.tif`. Finals
(used in figures):
- `QGIS project/cavetop_clean_masked.tif` -- FULL CAVE. Depth min -10.7 / mean 17.0 / max 36.3 m. CAVEAT:
  reliable near the fieldwork site, rougher west (source LiDAR horizontal accuracy degrades westward,
  Jameo de la Gente ~5-6 m off -- see Code/LiDAR/alignment_transforms.txt).
- `QGIS project/cavetop_clean_masked_PuertaFalsa.tif` -- PUERTA FALSA junction. RTK-anchored -- NO westward caveat.
- `QGIS project/cavetop_clean_masked_LaGente.tif` -- JAMEO DE LA GENTE / L5 (ceiling re-registered ~6-7 m).
  Surface used a -0.35 m drone correction (local ~+0.35 m drone bias vs RTK). Sanity: L5
  (E~649766, N~3227500) overburden ~13 m, matches the LiDAR cross-section.
  FIXED 2026-08-11: the `cavetop_clean_masked_LaGente` layer in `FieldworkReporting.qgz` was
  wired to the raw (unmasked) `cavetop_clean_LaGente.tif`, including a stale frozen-layer-list
  entry inside the `NWFieldworkArea` layout that a plain project-write didn't auto-correct.
  Repointed (PyQGIS `setDataSource` + a direct XML patch for the frozen entry, `.qgz.bak-20260811`
  kept as a pre-fix backup) and re-exported `NWFieldworkArea` + `OverviewFieldworkAreaNoLegend` to
  a scratch path -- both match the delivered thesis PDFs, so the frozen thesis figures were
  already correct; this only fixes reproducing them from the project going forward.
- `QGIS project/Jameos.shp` -- hand-digitized jameo/open-rim polygons, the nodata burn mask (EPSG:4083)

**Missing-CRS files (consolidated 2026-08-11):** several CloudCompare/other exports never got
an embedded or sidecar CRS, even though their coordinates are already correct EPSG:4083 -- they
just need it ASSIGNED (never reprojected) on load, or QGIS shows them as "unknown CRS".
- `QGIS project/caveheight_clean_laGente.tif` -- FIXED on disk 2026-08-11 via
  `gdal_edit.py -a_srs EPSG:4083` (a prior "Assign projection" in a live QGIS session never
  got written to the file -- verified with `gdalinfo`, stats unchanged, so this is a real fix
  now, not just a per-session workaround).
- `.prj` sidecars WRITTEN 2026-08-11 (copied WKT from the already-correct `Jameos.prj`) for:
  `LiDAR La Corona/Reregistered clouds/Gente_envelope.shp`,
  `LiDAR La Corona/Reregistered clouds/PuertaFalsaCleanEnvelope.shp`,
  `LiDAR La Corona/Tube Envelope - Cleaned and surface removed.shp` (used in 3 thesis-figure
  layouts -- the one that mattered most here), `QGIS project/envelope z-flat.shp`,
  `QGIS project/Legacy/All GPR surveys.shp`. All five now load with their CRS pre-set;
  no more "assign EPSG:4083 by hand" step needed for any of them.
- `Data/IGN data/DTM/MDT02-REGCAN95-HU28-1080-2-COB2.tif` -- 2m DTM tile 1 (IGN)
- `Data/IGN data/DTM/MDT02-REGCAN95-HU28-1080-4-COB2.tif` -- 2m DTM tile 2 (IGN)
- `Data/IGN data/Processed/MergedDTM.sdat` -- merged DTM (use this in QGIS)
DTM is styled as two layers: elevation color ramp (tv-a from qpt-city, 0-669m, transparent below 0.8m) on top of hillshade (azimuth 315, altitude 45), both with Multiply blend mode.

### Vector Layers
- `QGIS project/Envelope - x flat_new.shp`, `Envelope - y flat_new.shp`, `envelope z-flat.shp` -- lava tube outline from LiDAR; styled white (#FFFFFF)
- `QGIS project/Fieldwork Area.shp` -- fieldwork area bounding box polygon; dashed outline, annotation color
- `Data/GNSS/Cleaned/GPR_Lines.geojson` -- GPR survey lines (L2, L3, L5) converted from GNSS points via Code/QGIS/points_to_lines.py
- `Data/GNSS/Cleaned/GPR_FlowerPetals.geojson` -- flower petal GPR surveys (FP1, FP2, FP3)
- `Data/GNSS/Cleaned/CleanedGNSS_GPR_Lines.csv` -- original GPR GNSS points with Line and Meter columns
- `Data/GNSS/Cleaned/CleanedGNSS_GPR_FlowerPetals.csv` -- original flower petal GNSS points
- `QGIS project/Gravimetry Alessandro.shp` -- reference gravity survey from previous study
- `QGIS project/Legacy/` -- planning-stage shapefiles (`All GPR surveys.shp`, ContextLine,
  FlowerPetalLine, CentreLines, etc.) and the two pre-fieldwork `Fieldworkplanning*.qgz`
  projects, moved here 2026-08-11 (confirmed unreferenced by any current .qgz/thesis
  figure first). Not loaded by `FieldworkReporting.qgz` -- browse only if chasing history.

### Gravity Station Data
Loaded from gravimetry pipeline CSV outputs (Data/Gravimetry/Processed/). Stations styled by line with different symbols for base, tie, and regular stations. L3 and L4 share a base station -- styled as nested squares (outer L3 color, inner L4 color).

## Conventions

### CRS
EPSG:4083 (REGCAN95 / UTM zone 28N) -- project CRS for all layers and exports.
Note: IGN DTM tiles are in HU28 (UTM zone 28N) but QGIS reprojects on the fly.

### Color Palette (colorblind-friendly, Okabe-Ito inspired)
- L2: #0099FF (blue)
- L5: #00CC80 (green)
- L3: #FF5C00 (orange-red)
- L4 / Flower Petals: #FF4DB8 (magenta)
- Annotation (reserved): #FFC400 (gold)
- Cave outline: #FFFFFF (white)

### GPR Lines Script
`Code/QGIS/points_to_lines.py` converts GNSS CSVs to GeoJSON lines.
Order field varies by line: Time for L2 and FP1/2/3, Meter for L3 and L5.
Re-run if GNSS data changes; outputs to Data/GNSS/Cleaned/.

### Layer Blending for DEM
Elevation color layer sits ABOVE hillshade (not below). Both use Multiply blend mode.
Bilinear resampling on both layers to avoid blockiness.

## Status
Overburden roof-thickness maps (full cave, Puerta Falsa, La Gente) and the print-layout figures
(fieldwork overview, regional DEM, zoomed detail maps) are all DONE and in the thesis. See the
Rasters section for the overburden workflow and per-area caveats.
